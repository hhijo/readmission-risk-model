# Readmission Risk Prediction


This repository provides a baseline pipeline to predict 30-day readmission risk for hospital discharges. It includes preprocessing, model training (LightGBM/XGBoost), evaluation, and a basic Flask API for serving predictions.


## Quickstart
1. Create a Python environment and install dependencies:


```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
